# HubSpot Connector

> Status: stub — full content planned. See repo plan for what this file contains.

Webhooks, OAuth/Private App auth, rate limits (100 req/10s), gotchas (Operations Hub for many object types, engagements first-class). Sample webhook payload + extraction prompt link. ~400 words.
