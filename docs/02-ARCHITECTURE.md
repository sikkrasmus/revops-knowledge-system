# Architecture

> Status: stub — full content planned. See repo plan for what this file contains.

Three layers + three operations in detail. PR workflow walkthrough. Storage decisions (markdown + git, when to add a vector DB). Sub-scoping with nearest-file-wins AGENTS.md. ~1500 words.
